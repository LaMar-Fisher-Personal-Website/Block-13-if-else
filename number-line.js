
// Define the two numbers to be added
var num1 = 10;
var num2 = -1020;

// Calculate the sum
var sum = num1 + num2;

// Check if the sum is less than -1000
if (sum < -1000) {
  // If the sum is less than -1000, print the corresponding message
  console.log(sum + " is less than -1000");
} else if (sum > 1000) {
  // If the sum is greater than 1000, print the corresponding message
  console.log(sum + " is greater than 1000");
} else {
  // If the sum is between -1000 and 1000, print the sum itself
  console.log("The sum of " + num1 + " and " + num2 + " is " + sum);
}

// Define the two numbers to be added
var num1 = 5;
var num2 = -10;

// Calculate the sum
var sum = num1 + num2;

// Check if the sum is negative
if (sum < 0) {
    // If the sum is negative, print the corresponding message
    console.log(sum + " is a negative number");
} else if (sum > 0) {
    // If the sum is positive, print the corresponding message
    console.log(sum + " is a positive number");
} else {
    // If the sum is zero, print the corresponding message
    console.log("The sum of " + num1 + " and " + num2 + " is zero");
}

	// Define the two numbers to be added
    var num1 = 3;
    var num2 = -3;

    // Calculate the sum
    var sum = num1 + num2;

    // Check if the sum is equal to 0
    if (sum == 0) {
        // If the sum is equal to 0, print the corresponding message
        console.log(sum + " is equal to 0");
    } else if (sum > 0) {
        // If the sum is positive, print the corresponding message
        console.log(sum + " is a positive number");
    } else {
        // If the sum is negative, print the corresponding message
        console.log(sum + " is a negative number");
    }

    	// Define the two numbers to be added
		var num1 = 10;
		var num2 = 20;

		// Calculate the sum
		var sum = num1 + num2;

		// Check if the sum is larger than 0
		if (sum > 0) {
			// If the sum is larger than 0, print the corresponding message
			console.log(sum + " is larger than 0");
		} else if (sum < 0) {
			// If the sum is smaller than 0, print the corresponding message
			console.log(sum + " is smaller than 0");
		} else {
			// If the sum is equal to 0, print the corresponding message
			console.log("The sum of " + num1 + " and " + num2 + " is 0");
		}

        	// Define the two numbers to be added
		var num1 = 50;
		var num2 = 75;

		// Calculate the sum
		var sum = num1 + num2;

		// Check if the sum is greater than 100
		if (sum > 100) {
			// If the sum is greater than 100, print the corresponding message
			console.log(sum + " is greater than 100");
		} else if (sum < 100) {
			// If the sum is less than 100, print the corresponding message
			console.log(sum + " is less than 100");
		} else {
			// If the sum is equal to 100, print the corresponding message
			console.log("The sum of " + num1 + " and " + num2 + " is equal to 100");
		}
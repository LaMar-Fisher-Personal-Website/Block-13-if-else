
// Define the first set of two values
var value1a = "hello";
var value1b = "";

// Check if at least one of the pairs is truthy
if (value1a !== "" && value1b !== "" || value1a !== "" && value1b !== "") {
    // If at least one of the pairs is truthy, print true
    console.log(true);
} else {
    // If neither pair is truthy, print false
    console.log(false);
}

// Define the second set of two values
var value2a = 0;
var value2b = 7;

// Check if at least one of the pairs is truthy
if (value2a !== 0 && value2b !== 0 || value2a !== 0 && value2b !== 0) {
    // If at least one of the pairs is truthy, print true
    console.log(true);
} else {
    // If neither pair is truthy, print false
    console.log(false);
}
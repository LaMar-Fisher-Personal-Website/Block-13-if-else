
// Define the two parameters
var param1 = 5;
var param2 = 8;

// Check if both parameters are greater or equal to 5
if (param1 >= 5 && param2 >= 5) {
    // If both parameters are greater or equal to 5, print true
    console.log(true);
} else {
    // If either parameter is less than 5, print false
    console.log(false);
}